-- ============================================================
-- Color HRM マイグレーション 024
-- 確定シフトの休憩時間（分）。NULL=自動（拘束6時間超は60分）。
--   staff が「打刻・確定シフト」で各シフトの休憩分を変更できる。
--   休憩は運営（非授業）時間から差し引き、給料対象時間から除外される。
--   ※ 未実施でも動作する（自動ルールのみ・編集欄が出ないだけ）。
-- phpMyAdmin の SQL タブで1回実行。
-- ============================================================
SET NAMES utf8mb4;

ALTER TABLE shift_days ADD COLUMN break_minutes INT NULL;
-- ============================================================
