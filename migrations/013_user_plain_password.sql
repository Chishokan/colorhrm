-- ============================================================
-- Color HRM マイグレーション 013
-- 管理者エクスポート用に、設定時のパスワードを保存する列。
--   ※ 復元可能（平文）で保存します。adminの講師CSVエクスポートにのみ出力。
--   ※ 取り扱い注意：このDB/バックアップが漏れると全員のパスワードが露出します。
-- phpMyAdmin の SQL タブで1回実行。
-- ============================================================
SET NAMES utf8mb4;
ALTER TABLE users ADD COLUMN plain_password VARCHAR(255) NOT NULL DEFAULT '';
-- ============================================================
