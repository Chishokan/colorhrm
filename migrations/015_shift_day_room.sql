-- ============================================================
-- Color HRM マイグレーション 015
-- 確定シフトに「教室」を持たせる。
--   申請中シフトは教室を持たず（配属教室すべてのシフト表に表示）、
--   staff が確定する際に「どの教室で組むか」を選び、その教室にのみ確定シフトを表示する。
-- phpMyAdmin の SQL タブで1回実行。
-- ============================================================
SET NAMES utf8mb4;
ALTER TABLE shift_days ADD COLUMN room VARCHAR(50) NOT NULL DEFAULT '' AFTER work_date;
-- ============================================================
